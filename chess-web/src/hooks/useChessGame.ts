// Stub: Hook para abstrair a instância do chess.js e fornecer métodos (move, reset, undo)
// O desenvolvedor deve instanciar o objeto Chess aqui e talvez usar Zustand para persistir o estado de forma global.

export function useChessGame() {
  // const [chess] = useState(new Chess());
  // function makeMove(...) { ... }
  
  return {
    // expose métodos e propriedades
  };
}
